import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Aplikasi dapat dimuat', (WidgetTester tester) async {
    // Test ini membutuhkan Firebase mock untuk dijalankan
    // Lihat dokumentasi Firebase Test untuk setup
    expect(true, isTrue);
  });
}
